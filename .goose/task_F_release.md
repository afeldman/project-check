# Task F: GoReleaser + GitHub Actions + fish-food placeholder

## Goal
Set up release infrastructure for project-check.

## Files to create

### .goreleaser.yaml
GoReleaser v2 config. Pattern: copy from `/Users/anton.feldmann/Projects/priv/pkg/batch-cost/.goreleaser.yaml` and adapt.

Key changes:
- project_name: project-check
- builds: binary name `project-check`, CGO_ENABLED=0
- archives: name template `{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}`
- after-hook: `gotofish gen --release project-check`
- No windows build

### .github/workflows/release.yml
GitHub Actions workflow. Pattern: copy from `/Users/anton.feldmann/Projects/priv/pkg/batch-cost/.github/workflows/release.yml` and adapt.

Key changes:
- project-check binary name
- Install gotofish step (before goreleaser):
  ```yaml
  - name: Install gotofish
    run: |
      curl -fsSL https://raw.githubusercontent.com/afeldman/scripts/master/install.sh | sh
      echo "$HOME/.local/bin" >> $GITHUB_PATH
  ```
- Secrets needed: `FISH_FOOD_TOKEN` (for cross-repo push to fish-food)

### fish-food placeholder
Create `/Users/anton.feldmann/Projects/priv/pkg/fish-food/Food/project-check.lua` as placeholder:

```lua
-- project-check: LLM-powered project standards checker
-- Generated by gotofish gen --release project-check
-- https://github.com/afeldman/project-check

food = {
    name = "project-check",
    description = "LLM-powered project standards checker",
    homepage = "https://github.com/afeldman/project-check",
    version = "0.0.0",
    packages = {
        {
            os = "darwin",
            arch = "amd64",
            url = "",
            sha256 = "",
            resources = {
                {
                    path = "project-check",
                    installpath = "bin/project-check",
                    executable = true
                }
            }
        }
    }
}
```

## Constraints
- YAML must be valid
- Workflow must use actions/checkout@v4, goreleaser-action@v6
